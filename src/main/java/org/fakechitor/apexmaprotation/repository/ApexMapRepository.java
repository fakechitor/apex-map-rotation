package org.fakechitor.apexmaprotation.repository;

import org.fakechitor.apexmaprotation.model.ApexMap;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ApexMapRepository extends JpaRepository<ApexMap, Long> {
}
